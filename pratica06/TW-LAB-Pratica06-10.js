let values = []

function addValue(value) {
  values.push(value)
}

function avg (values) {
  for (var i = 0; i < values.length; i++) {
    let avg += values[i]
    return avg / values.lenght
  }
}

function init () {
  let value = documet.getElementById('inpt')
}
